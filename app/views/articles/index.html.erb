<h1>Article</h1>
<ul>
    <% @articles.each do |article| %>
        <li>
            # <a href="<%= article_path(article) %>">
            #     <%= article.title %>
            # </a>
            <%= link_to article.title, article %>
        </li>
    <% end %>
</ul>